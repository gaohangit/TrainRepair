<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ydzbinfo.emis.trainRepair.repairworkflow.dao.NodeRecordExtraInfoMapper">

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        ${tableAlias}S_NODERECORDID as "${columnPrefix}nodeRecordId",
        ${tableAlias}S_TYPE as "${columnPrefix}type",
        ${tableAlias}S_VALUE as "${columnPrefix}value"
    </sql>

</mapper>
