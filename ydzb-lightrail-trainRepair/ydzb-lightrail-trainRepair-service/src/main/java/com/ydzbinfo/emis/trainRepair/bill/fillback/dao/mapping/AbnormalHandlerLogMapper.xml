<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ydzbinfo.emis.trainRepair.bill.fillback.dao.AbnormalHandlerLogMapper">

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        ${tableAlias}S_ABNORMALHANDLERLOGID as "${columnPrefix}abnormalHandlerLogId",
        ${tableAlias}S_CHECKLISTSUMMARYID as "${columnPrefix}checklistSummaryId",
        ${tableAlias}S_DAYPLANID as "${columnPrefix}dayPlanId",
        ${tableAlias}S_TRAINSETNAME as "${columnPrefix}trainsetName",
        ${tableAlias}S_MAINCYC as "${columnPrefix}mainCyc",
        ${tableAlias}S_PACKETCODE as "${columnPrefix}packetCode",
        ${tableAlias}S_PACKETNAME as "${columnPrefix}packetName",
        ${tableAlias}S_ITEMCODE as "${columnPrefix}itemCode",
        ${tableAlias}S_ITEMNAME as "${columnPrefix}itemName",
        ${tableAlias}S_STAFFCODE as "${columnPrefix}staffCode",
        ${tableAlias}S_STAFFNAME as "${columnPrefix}staffName",
        ${tableAlias}D_CREATETIME as "${columnPrefix}createTime",
        ${tableAlias}C_HANDLERTYPE as "${columnPrefix}handlerType",
        ${tableAlias}S_IPADDRESS as "${columnPrefix}ipAddress",
        ${tableAlias}S_HOSTNAME as "${columnPrefix}hostname",
        ${tableAlias}S_REMARKS as "${columnPrefix}remarks",
        ${tableAlias}C_USEFLAG as "${columnPrefix}useFlag"
    </sql>

</mapper>
