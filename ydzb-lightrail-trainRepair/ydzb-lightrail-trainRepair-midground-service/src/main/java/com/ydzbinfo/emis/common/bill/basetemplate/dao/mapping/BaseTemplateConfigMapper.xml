<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ydzbinfo.emis.common.bill.basetemplate.dao.BaseTemplateConfigMapper">

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        ${tableAlias}S_ID as "${columnPrefix}id",
        ${tableAlias}S_TEMPLATETYPECODE as "${columnPrefix}templateTypeCode",
        ${tableAlias}S_QUERYCONDITION as "${columnPrefix}queryCondition"
    </sql>

</mapper>
